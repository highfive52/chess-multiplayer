[pytest]
markers =
    integration: mark tests that require external services (docker-compose postgres/redis)
filterwarnings =
    ignore::starlette.exceptions.StarletteDeprecationWarning
    ignore::UserWarning:pydantic._internal._config
