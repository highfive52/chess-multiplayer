{
  "name": "chess-multiplayer-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others -n \"backend,frontend\" -c \"bgBlue,bgGreen\" \"uv run uvicorn backend.main:asgi_app --reload --port 8000\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
